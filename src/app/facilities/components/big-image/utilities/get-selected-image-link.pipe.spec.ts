import { GetSelectedImageLinkPipe } from './get-selected-image-link.pipe';

describe('GetSelectedImageLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new GetSelectedImageLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
