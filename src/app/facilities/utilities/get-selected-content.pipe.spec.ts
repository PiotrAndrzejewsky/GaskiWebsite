import { GetSelectedContentPipe } from './get-selected-content.pipe';

describe('GetSelectedContentPipe', () => {
  it('create an instance', () => {
    const pipe = new GetSelectedContentPipe();
    expect(pipe).toBeTruthy();
  });
});
