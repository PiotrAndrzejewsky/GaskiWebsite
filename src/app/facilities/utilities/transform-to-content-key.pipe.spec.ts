import { TransformToContentKeyPipe } from './transform-to-content-key.pipe';

describe('TransformToContentKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformToContentKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
