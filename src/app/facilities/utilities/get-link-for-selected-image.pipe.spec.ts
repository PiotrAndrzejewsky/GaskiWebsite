import { GetLinkForSelectedImagePipe } from './get-link-for-selected-image.pipe';

describe('GetLinkForSelectedImagePipe', () => {
  it('create an instance', () => {
    const pipe = new GetLinkForSelectedImagePipe();
    expect(pipe).toBeTruthy();
  });
});
