import { GetVisibleContentPipe } from './get-visible-content.pipe';

describe('GetVisibleContentPipe', () => {
  it('create an instance', () => {
    const pipe = new GetVisibleContentPipe();
    expect(pipe).toBeTruthy();
  });
});
