import { GetSelectedImageTitlePipe } from './get-selected-image-title.pipe';

describe('GetSelectedImageTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new GetSelectedImageTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
