import { GetImageLinkPipe } from './get-image-link.pipe';

describe('GetImageLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new GetImageLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
