import { AmountOfDaysPipe } from './amount-of-days.pipe';

describe('AmountOfDaysPipe', () => {
  it('create an instance', () => {
    const pipe = new AmountOfDaysPipe();
    expect(pipe).toBeTruthy();
  });
});
